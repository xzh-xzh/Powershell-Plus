<Window x:Class="PowerShellPlus.Views.CommandEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加命令" 
        Height="550" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        MinHeight="450" MinWidth="500"
        Background="{StaticResource BackgroundMediumBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock x:Name="DialogTitle"
                   Grid.Row="0" 
                   Text="➕ 添加命令" 
                   FontSize="22" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}"
                   Margin="0,0,0,24"/>

        <!-- 表单内容 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 命令名称 -->
                <TextBlock Text="命令名称 *" 
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="13"
                           Margin="0,0,0,8"/>
                <TextBox x:Name="NameBox"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Padding="12,10"
                         FontSize="14"
                         MaxLength="30"/>
                <TextBlock Text="给命令起一个简短的名称，如：清理临时文件" 
                           Foreground="{StaticResource TextMutedBrush}"
                           FontSize="11"
                           Margin="0,4,0,0"/>

                <!-- 图标选择 -->
                <TextBlock Text="图标" 
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="13"
                           Margin="0,20,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Background="{StaticResource BackgroundDarkBrush}"
                            CornerRadius="8"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1">
                        <TextBox x:Name="IconBox"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="{StaticResource TextPrimaryBrush}"
                                 Padding="10,8"
                                 MaxLength="4"
                                 TextAlignment="Center"
                                 FontSize="28"
                                 VerticalContentAlignment="Center"/>
                    </Border>
                    
                    <StackPanel Grid.Column="1" Margin="16,0,0,0">
                        <TextBlock Text="选择常用图标：" 
                                   Foreground="{StaticResource TextMutedBrush}" 
                                   FontSize="11" 
                                   Margin="0,0,0,8"/>
                        <WrapPanel>
                            <Button Content="⚡" Style="{StaticResource QuickCommandButtonStyle}" Padding="10,6" FontSize="16" Click="EmojiButton_Click" Margin="0,0,6,6"/>
                            <Button Content="📁" Style="{StaticResource QuickCommandButtonStyle}" Padding="10,6" FontSize="16" Click="EmojiButton_Click" Margin="0,0,6,6"/>
                            <Button Content="🔧" Style="{StaticResource QuickCommandButtonStyle}" Padding="10,6" FontSize="16" Click="EmojiButton_Click" Margin="0,0,6,6"/>
                            <Button Content="📊" Style="{StaticResource QuickCommandButtonStyle}" Padding="10,6" FontSize="16" Click="EmojiButton_Click" Margin="0,0,6,6"/>
                            <Button Content="🚀" Style="{StaticResource QuickCommandButtonStyle}" Padding="10,6" FontSize="16" Click="EmojiButton_Click" Margin="0,0,6,6"/>
                            <Button Content="💾" Style="{StaticResource QuickCommandButtonStyle}" Padding="10,6" FontSize="16" Click="EmojiButton_Click" Margin="0,0,6,6"/>
                            <Button Content="🌐" Style="{StaticResource QuickCommandButtonStyle}" Padding="10,6" FontSize="16" Click="EmojiButton_Click" Margin="0,0,6,6"/>
                            <Button Content="🧹" Style="{StaticResource QuickCommandButtonStyle}" Padding="10,6" FontSize="16" Click="EmojiButton_Click" Margin="0,0,6,6"/>
                            <Button Content="📦" Style="{StaticResource QuickCommandButtonStyle}" Padding="10,6" FontSize="16" Click="EmojiButton_Click" Margin="0,0,6,6"/>
                            <Button Content="🔍" Style="{StaticResource QuickCommandButtonStyle}" Padding="10,6" FontSize="16" Click="EmojiButton_Click" Margin="0,0,6,6"/>
                            <Button Content="⚙️" Style="{StaticResource QuickCommandButtonStyle}" Padding="10,6" FontSize="16" Click="EmojiButton_Click" Margin="0,0,6,6"/>
                            <Button Content="🔄" Style="{StaticResource QuickCommandButtonStyle}" Padding="10,6" FontSize="16" Click="EmojiButton_Click" Margin="0,0,6,6"/>
                        </WrapPanel>
                    </StackPanel>
                </Grid>

                <!-- PowerShell 命令 -->
                <TextBlock Text="PowerShell 命令 *" 
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="13"
                           Margin="0,20,0,8"/>
                <Border Background="#1A1A2E"
                        BorderBrush="#2D3748"
                        BorderThickness="1"
                        CornerRadius="8">
                    <TextBox x:Name="CommandBox"
                             Background="Transparent"
                             Foreground="#7DD3FC"
                             CaretBrush="#7DD3FC"
                             FontFamily="Cascadia Code, Consolas, Courier New"
                             FontSize="13"
                             Padding="14"
                             MinHeight="120"
                             MaxHeight="200"
                             AcceptsReturn="True"
                             AcceptsTab="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             BorderThickness="0"/>
                </Border>
                <TextBlock Text="输入要执行的 PowerShell 命令，支持多行" 
                           Foreground="{StaticResource TextMutedBrush}"
                           FontSize="11"
                           Margin="0,4,0,0"/>

                <!-- 描述 -->
                <TextBlock Text="描述（可选）" 
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="13"
                           Margin="0,20,0,8"/>
                <TextBox x:Name="DescriptionBox"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Padding="12,10"
                         FontSize="14"
                         MaxLength="100"/>
                <TextBlock Text="简短描述命令的用途，鼠标悬停时会显示" 
                           Foreground="{StaticResource TextMutedBrush}"
                           FontSize="11"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </ScrollViewer>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,24,0,0">
            <Button Content="取消" 
                    Style="{StaticResource SecondaryButtonStyle}"
                    Click="Cancel_Click"
                    Width="100"/>
            <Button Content="保存" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="Save_Click"
                    Margin="12,0,0,0"
                    Width="100"/>
        </StackPanel>
    </Grid>
</Window>

