<Window x:Class="PowerShellPlus.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置 - PowerShell Plus" 
        Height="480" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundMediumBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" 
                   Text="⚙️ 设置" 
                   FontSize="24" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}"
                   Margin="0,0,0,24"/>

        <!-- 设置内容 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- API 设置组 -->
                <Border Background="{StaticResource BackgroundDarkBrush}" 
                        CornerRadius="10" 
                        Padding="16" 
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="🔑 API 配置" 
                                   FontSize="14" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   Margin="0,0,0,16"/>

                        <!-- API Key -->
                        <TextBlock Text="API Key" 
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="12"
                                   Margin="0,0,0,6"/>
                        <PasswordBox x:Name="ApiKeyBox"
                                     PasswordChanged="ApiKeyBox_PasswordChanged"
                                     Background="{StaticResource BackgroundMediumBrush}"
                                     Foreground="{StaticResource TextPrimaryBrush}"
                                     BorderBrush="{StaticResource BorderBrush}"
                                     Padding="10,8"
                                     FontSize="13"/>

                        <!-- API Base URL -->
                        <TextBlock Text="API Base URL" 
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="12"
                                   Margin="0,16,0,6"/>
                        <TextBox x:Name="ApiBaseUrlBox"
                                 Style="{StaticResource ModernTextBoxStyle}"
                                 Padding="10,8"/>
                        <TextBlock Text="支持 OpenAI 兼容的 API（如 DeepSeek、智谱等）" 
                                   Foreground="{StaticResource TextMutedBrush}"
                                   FontSize="11"
                                   Margin="0,4,0,0"/>

                        <!-- Model -->
                        <TextBlock Text="模型名称" 
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="12"
                                   Margin="0,16,0,6"/>
                        <TextBox x:Name="ModelBox"
                                 Style="{StaticResource ModernTextBoxStyle}"
                                 Padding="10,8"/>
                    </StackPanel>
                </Border>

                <!-- 高级设置组 -->
                <Border Background="{StaticResource BackgroundDarkBrush}" 
                        CornerRadius="10" 
                        Padding="16">
                    <StackPanel>
                        <TextBlock Text="🎛️ 高级设置" 
                                   FontSize="14" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   Margin="0,0,0,16"/>

                        <!-- Temperature -->
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="60"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Temperature (创造性)" 
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="12"
                                           Margin="0,0,0,6"/>
                                <Slider x:Name="TemperatureSlider"
                                        Minimum="0" Maximum="1" 
                                        TickFrequency="0.1"
                                        IsSnapToTickEnabled="True"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" 
                                       Text="{Binding Value, ElementName=TemperatureSlider, StringFormat='{}{0:F1}'}"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       FontSize="14"
                                       VerticalAlignment="Bottom"
                                       HorizontalAlignment="Right"/>
                        </Grid>

                        <!-- Max Tokens -->
                        <TextBlock Text="最大 Token 数" 
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="12"
                                   Margin="0,0,0,6"/>
                        <TextBox x:Name="MaxTokensBox"
                                 Style="{StaticResource ModernTextBoxStyle}"
                                 Padding="10,8"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,24,0,0">
            <Button Content="取消" 
                    Style="{StaticResource SecondaryButtonStyle}"
                    Click="Cancel_Click"
                    Width="100"/>
            <Button Content="保存" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="Save_Click"
                    Margin="12,0,0,0"
                    Width="100"/>
        </StackPanel>
    </Grid>
</Window>
